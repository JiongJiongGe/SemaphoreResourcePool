<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="env.semaphore.semaphore.mapper.shiro.PermissionMapper">

    <sql id="tableName">
        t_permission
    </sql>

    <sql id="selectColumns">
        *
    </sql>

    <select id="queryList" resultType="env.semaphore.semaphore.domain.shiro.PermissionDo">
        SELECT
        <include refid="selectColumns" />
        FROM
        <include refid="tableName" />
    </select>

</mapper>